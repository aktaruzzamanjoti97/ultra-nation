import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <div>
            <h2 className="heading-style">Country Details (SPA)</h2>
        </div>
    );
};

export default Header;